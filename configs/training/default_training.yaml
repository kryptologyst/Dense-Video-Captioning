# @package training

_target_: src.train.trainer.VideoCaptionTrainer

# Training parameters
max_epochs: 100
save_every: 10
learning_rate: 1e-4
weight_decay: 1e-5
min_lr: 1e-6

# Mixed precision
mixed_precision: true

# Logging
log_dir: "logs"
checkpoint_dir: "checkpoints"
